## Getting Started

Install http-server:
```bash
npm install -g http-server
```

Run the development server:

```bash
npm run dev
```

Run the project on a separate terminal:

```bash
npm run start
```